{
  "name": "subscribe-proxy",
  "version": "0.1.0",
  "private": true,
  "description": "Generic Cloudflare Worker that proxies subscribe form submissions to Resend's audience contacts API.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241218.0",
    "typescript": "^5.4.0",
    "wrangler": "^4.84.0"
  }
}
